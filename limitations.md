# 已知局限

1. 当前开销数字来自一台 Windows 11 笔记本 CPU。它适合证明 P1 基础设施达到门槛，不能替代目标 GPU 上的补充测量。
2. 每族使用 7 个配对重复。MOT 的 bootstrap 95% 区间已由 -4.25%～+14.26% 收窄至 +0.51%～+5.37%。MOE 仍有一次明显的 CPU 调度异常值，令其区间上界达到 +11.08%；中位数为 +2.97%，按任务书的“训练减速 <10%”口径通过。原始值全部保留，没有删除异常点。
3. 模型从固定随机种子初始化，测量的是采集基础设施开销，不报告检测精度。
4. 默认每 2 个 batch 采样一次。提高刷新频率会增加磁盘写入和 schema 校验开销。
5. 腾讯 LATENT 配置将 `router_init_std=0.0` 与 `residual_init=0.0`，因此短基准内 4 个专家保持对称的 0.25 权重。这是模型初始化与短训练预算的结果，不能用于判断最终检测精度。研究路由分化需要独立的长训练和初始化消融。
6. 腾讯 LATENT 模型在构建和 forward 后会缓存仍连接计算图的诊断张量，导致训练器复制 EMA 或 checkpoint 时失败。P1 的外部 trainer 子类在复制前、每个训练 batch 后清理这些临时缓存；没有修改腾讯源码或核心 forward。
7. 实时面板仅监听本机，由浏览器每秒轮询 `latest.json`。它不包含认证和远程部署功能。
